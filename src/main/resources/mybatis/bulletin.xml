<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.bulletin.BulletinDAOInter">
    <insert id="create" parameterType="dev.mvc.bulletin.BulletinVO">
    INSERT INTO bulletin(bulletinidx, bulletinname, bulletintext, bulletinpicture)
    VALUES (BULLETIN_BULLETINIDX_SEQ.nextval, #{bulletinname}, #{bulletintext}, #{bulletinpicture});
    </insert>

  <select id="read" resultType="dev.mvc.bulletin.BulletinVO" parameterType="int">
    SELECT bulletinidx, bulletinname, bulletintext, bulletinpicture
    FROM bulletin
    WHERE bulletinidx = #{bulletinidx};
  </select>  
  
    <update id="update" parameterType="dev.mvc.bulletin.BulletinVO">
    UPDATE bulletin
    SET bulletinname=#{bulletinname}, bulletintext=#{bulletintext}, bulletinpicture=#{bulletinpicture}
    WHERE bulletinidx=#{bulletinidx};
    </update>
    
    <delete id="delete" parameterType="int">
    DELETE FROM bulletin
    WHERE bulletinidx=#{bulletinidx}
    </delete>
</mapper>